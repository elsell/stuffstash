import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";
import BusyContent from "./busy-button-content.svelte";

export {
	Root,
	BusyContent,
	type ButtonProps as Props,
	//
	Root as Button,
	BusyContent as BusyButtonContent,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
