/** Native semantics supported by the reviewed @expo/ui55.0.17 registry patch. */
export function nativeContentDescription(contentDescription: string) {
  return { $type: 'contentDescription', contentDescription };
}
