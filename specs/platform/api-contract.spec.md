# API Contract Spec

## Purpose

Stuff Stash needs REST APIs that are predictable, secure, documented from source, and usable by web, mobile, MCP, and future adapters.

## Scope

This spec covers cross-cutting API rules for HTTP REST endpoints:

- REST resource design.
- OpenAPI generation.
- Response body shape.
- Error body shape.
- Pagination.
- Authentication and authorization contract behavior.
- Security hardening for API responses.

This spec does not define domain-specific endpoints. Domain specs must define those resources before implementation.

## Requirements

- REST endpoints must follow standard REST conventions.
- Endpoint paths must use stable resource nouns.
- HTTP methods must match the operation being performed:
  - `GET` retrieves resources.
  - `POST` creates resources or starts non-idempotent operations.
  - `PUT` replaces resources when full replacement is supported.
  - `PATCH` partially updates resources when partial updates are supported.
  - `DELETE` deletes or archives resources when allowed by the domain spec.
- Successful responses must use appropriate HTTP status codes.
- Error responses must use appropriate HTTP status codes.
- API behavior must be consistent across all domains unless a spec documents a domain-specific exception.
- Tenant identity and authorization behavior must be represented in the API contract where relevant.
- Authentication and authorization must remain behind ports and adapters.
- Domain logic must not depend on HTTP framework types.

## OpenAPI

- REST API documentation must be generated from implementation source or a checked contract artifact that is itself generated by pinned tooling.
- OpenAPI must not be handwritten as a separate manual artifact.
- The first REST implementation must use Huma for FastAPI-style code-first OpenAPI generation unless a future spec documents a better fit before route implementation begins.
- Spec-first generators such as ogen or oapi-codegen may be chosen instead only if a spec explains why a checked OpenAPI contract should drive generated server/client code.
- The selected OpenAPI tooling, version, generation command, and output path must be specified before domain REST endpoints are added.
- Swagger-style interactive documentation may be exposed locally and in non-production environments when a spec permits it.
- Production exposure of interactive API documentation must be explicitly specified and security reviewed before it is enabled.
- Generated OpenAPI output must include paths, methods, request bodies, response bodies, error bodies, status codes, authentication schemes, and pagination parameters.
- The generated OpenAPI artifact must be reproducible from pinned dependencies and tool versions.
- Client API code must be generated from the OpenAPI contract for web and mobile clients unless a spec explicitly justifies an exception.
- Generated client code must not be edited manually.
- Generated OpenAPI output and generated client code must have drift checks in pre-commit or CI once generation exists.

## TypeScript Client Generation

- TypeScript API contract types must be generated with `openapi-typescript`.
- The pinned generator version is owned by `specs/platform/tooling-versions.spec.md`.
- The checked OpenAPI artifact for the TypeScript client package lives at `packages/api-client/openapi.json` until a future spec moves or publishes it.
- Generated TypeScript schema output lives at `packages/api-client/src/generated/schema.d.ts`.
- The generation command is `pnpm --dir packages/api-client generate`, also exposed from the repository root as `pnpm api-client:generate`.
- The generated schema file must not be edited by hand.
- Hand-written TypeScript client wrappers may use `openapi-fetch` over the generated schema, but they remain transport adapters and must not become frontend domain models.
- Web and mobile clients must map generated DTOs into client-side domain models at their adapter boundary when they need product behavior, validation, or presentation state.

## Generated SDK Boundary

- Generated SDK code is transport infrastructure.
- Generated SDK DTOs are API contract types, not frontend domain models.
- Web and mobile clients must keep their own frontend domain models where product behavior needs client-side concepts, validation, presentation state, or offline behavior.
- Client application code must access generated SDKs through adapters or ports.
- DTO-to-domain mapping must live at the client adapter boundary.
- Frontend domain logic must not depend directly on generated SDK DTOs.
- Generated SDKs must expose the consistent response envelope, error envelope, pagination metadata, authentication behavior, and request identifiers defined by this spec.
- SDK generation must be reproducible through pinned tools and committed commands.

## Response Format

Successful JSON responses must use a consistent envelope unless a spec explicitly justifies a different shape:

```json
{
  "data": {},
  "meta": {
    "requestId": "req_...",
    "tenantId": "tenant_..."
  }
}
```

- `data` contains the returned resource, collection, or operation result.
- `meta.requestId` contains the request correlation identifier when available.
- `meta.tenantId` contains the tenant identifier when exposing it is useful and safe.
- Empty successful responses may use HTTP `204 No Content` when there is no useful body.
- Field names in JSON must use `camelCase`.
- Response bodies must not leak internal package names, stack traces, SQL errors, SpiceDB internals, OIDC provider internals, secrets, tokens, filesystem paths, or infrastructure details.

## Browser CORS

Separate browser clients may call the API directly.

- Browser CORS access must be deny-by-default.
- Allowed browser origins must come from environment-backed configuration.
- CORS must use exact origin matching, not wildcard matching.
- CORS preflight responses must allow only the methods and headers the public API needs.
- CORS responses must not allow credentials in the first web tracer bullet because browser clients use bearer tokens.
- Missing or disallowed origins must receive normal API responses without CORS allow headers.

## Error Format

Error responses must use a consistent envelope:

```json
{
  "error": {
    "code": "resource_not_found",
    "message": "Resource not found.",
    "details": []
  },
  "meta": {
    "requestId": "req_..."
  }
}
```

- `error.code` must be a stable enumeration value.
- Application errors must use typed internal error categories for validation, conflict, precondition failure, authentication, authorization, and not found behavior. HTTP adapters map those categories to safe public envelopes.
- `error.message` must be safe to show to a user.
- `error.details` may include structured validation or conflict details when safe.
- Error details must not disclose sensitive internal information.
- Authentication failures must not reveal whether a user, tenant, provider account, or credential exists.
- Authorization failures must not reveal hidden resources across tenants or access boundaries.
- The API must prefer stable error codes over string matching on messages.

## Pagination

- Collection endpoints must support real pagination from the start.
- Collection endpoints must not return unbounded result sets.
- Cursor pagination is the default pagination strategy for domain collections.
- Page-number pagination may be used only when a spec justifies it for a specific read model.
- Pagination parameters must be consistent across collection endpoints.
- The default request parameters are:
  - `limit`: requested page size.
  - `cursor`: opaque cursor returned by a previous response.
- Responses for paginated collections must include pagination metadata:

```json
{
  "data": [],
  "meta": {
    "requestId": "req_...",
    "pagination": {
      "limit": 50,
      "nextCursor": null,
      "hasMore": false
    }
  }
}
```

- Cursors must be opaque to clients.
- The API must define maximum and default page sizes through environment-backed configuration.
- Pagination must preserve tenant and authorization boundaries.
- Pagination must be stable enough that clients do not skip or duplicate resources during ordinary browsing.

## Security Contract

- Every authenticated endpoint must reject unauthenticated requests.
- Every authorized endpoint must reject unauthorized requests.
- Every tenant-scoped endpoint must reject cross-tenant access.
- Security-sensitive endpoints must have adversarial end-to-end tests before implementation is considered complete.
- Tests must cover successful access, unauthenticated access, unauthorized access, cross-tenant access, wrong-role access, malformed credentials, expired credentials, and privilege escalation attempts where applicable.
- HTTP error bodies for authentication, authorization, and tenant isolation failures must be intentionally bland and safe.
- API responses must include security headers once the HTTP adapter is implemented.
- CORS behavior must be explicit, environment-configured, and tested before browser clients depend on it.
- API responses must include at least `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer`, `X-Frame-Options: DENY`, and `Content-Security-Policy: default-src 'none'; frame-ancestors 'none'` unless a future spec justifies a narrower route-specific exception.
- Server read header timeout, read timeout, write timeout, idle timeout, and maximum JSON request body bytes must come from environment-backed configuration with safe defaults.
- JSON request body limits must apply before the Huma route handler decodes request bodies.
- JSON request body limits must allow bodies whose encoded byte length is exactly the configured maximum and reject bodies larger than that maximum.
- Oversized JSON request bodies must return HTTP `413 Request Entity Too Large` with the safe error envelope code `payload_too_large`, message `Request body too large.`, and no sensitive implementation details.
- Attachment upload routes may use a larger body limit derived from the configured attachment byte limit, but must still enforce a deterministic maximum.
- Attachment upload body limits must be route-specific and must not raise the configured maximum JSON body size for ordinary API endpoints.
- Rate limiting remains required before public or multi-user deployment, but is not implemented in this hardening slice.

## Verification

- API contract tests must verify response envelopes, error envelopes, pagination metadata, and status codes.
- OpenAPI generation must be part of the normal build or verification workflow once REST domain endpoints exist.
- Generated OpenAPI output must be checked for drift in CI or pre-commit once the generation workflow exists.
- Security-focused end-to-end tests must cover every authentication and authorization boundary exposed by the REST adapter.
