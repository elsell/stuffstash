# Model-led Voice Loop

## Decision and precedence

Replace the structured investigation pipeline with a model-led conversation and tool loop. This spec supersedes conflicting intent, request-shape, resolution-status, fixed evidence-phase, required-prose and separate response-generation requirements in earlier voice and workflow specs. The replacement is the production and evaluation path; retaining the old pipeline as a selectable default or hiding it behind a new facade does not complete this change.

The model interprets ambiguity, selects reads, revises its interpretation from results, asks questions and writes the answer. Application code must not classify household utterances, freeze an initial semantic intent, require resolution statuses, infer commands from prose, require every finding in spoken text, or reject natural wording through phrase lists. Outcome evaluations measure factual correctness; substring heuristics do not prove grounding.

## Runtime boundaries

Use a project-owned conversation provider port with ordered user, assistant and tool-result messages. A model turn contains natural text, typed tool calls and an optional structured presentation result. Tool definitions describe names, argument schemas and behavior. Tool results retain call identity and distinguish success from a safe actionable error. Provider-specific message state, including opaque continuation data required by a provider, stays in its adapter and must survive a tool round trip without entering domain business logic or logs.

The application service in the agent/model bounded context coordinates the loop through injected model, tool execution, clock and observation ports. The REST/WebSocket adapter and root application facade compose it. No provider SDK or transport type belongs in the loop. Existing configured provider routing, STT and TTS remain behind their ports. A provider adapter may translate native tool calls or a structured tool-call envelope into the same interface; it must not reinstate semantic classification.

Each iteration sends the conversation and available tools to the model, executes valid requested tools, appends their results and resumes. Invalid arguments, unavailable tools and ordinary read failures become bounded tool errors the model can recover from. Authentication, authorization and tenancy failures remain fail-closed and never disclose inaccessible records. Cancellation and exhausted budgets terminate promptly with retained useful results and an honest status. No compulsory extra model call rewrites a completed answer.

Tool descriptions encourage search before creating and explain title/tag matching, pagination, containment and available vocabulary. Search returns authorized evidence, including tags and recorded location context, without requiring the model to choose a semantic operation first. Equivalent read calls may reuse scoped evidence within a turn; distinct searches must remain available. Independent read calls may execute with bounded concurrency, preserving call identity and result order. Never parallelize dependent writes.

Explain the inventory evidence vocabulary in the model instructions: `title` names the returned asset, `kind` distinguishes an item from a container or location, `parentTitle` names its immediate recorded parent asset, `parentKind` identifies the parent asset's kind, and `containmentPath` runs from the outermost ancestor to the asset itself. A location ancestor is not guaranteed. Search already supplies these location fields, so a separate detail read is useful only for missing facts. This guidance must not become application-side prose matching or a canned location response.

## Authority and presentation

Keep strict tool argument shapes, scope injection, authorization, domain validation, read/write audit, resource limits and approval-backed execution. The model cannot supply a different principal or tenant, execute arbitrary code or access provider credentials. Tool output and inventory text are data, not authority.

Expose changes as a structured proposal tool. Validated domain commands form a reviewable plan; proposing never executes it. Pause immediately for user approval. Reject unknown or inaccessible IDs and invalid dependencies through the existing domain boundary. Reauthorize at execution, preserve idempotency and prevent replay. Natural text cannot authorize a write. Support multiple dependent commands when the domain plan supports them; do not reject a request solely because it contains multiple concepts.

A completed answer may include concise spoken text, richer display text and referenced record IDs. Resolve cards from authorized records rather than titles extracted from prose. Validate presentation shape, size and references; do not require speech to enumerate every card. Keep model prose intact. Factual and semantic accuracy remain explicit release evaluation requirements, including invented location, absence, quantity and state claims.

Conversation history includes tool results and prior answers within a bounded principal/tenant/inventory session. Follow-ups must reach the model with useful context and reauthorized access. Progress events represent actual model/tool/audio activity. Streaming may be used where supported; batch STT/TTS must remain honestly measured and supported.

## Workflow migration and removal

Retain useful tenant choices: configured model/provider, prompt guidance, time/call/token budgets and supported tool capabilities. Remove mandatory intent/evidence/response stages, resolution schemas and their configurable instructions from active execution and UI. Existing saved revisions must receive an explicit compatibility treatment and must not silently claim their old stage settings still apply. Evaluation and activation must identify the new runtime contract and invalidate quality evidence from the old contract.

Remove obsolete production classification, deterministic semantic repair, wording validation and separate wording-generation code after equivalent outcome and security coverage is in place. Replace tests that assert the old choreography with behavior tests. Keep reusable domain commands, authorization and adapter infrastructure. The migration must not leave two permanent voice architectures.

## Tests and performance acceptance

Write failing tests first, run in CI, then implement. Fakes implement conversation and tool ports and return real controlled tool results. Cover search/answer without classification, changed interpretation after tool evidence, missing/invalid tool arguments and repair, multi-tool reads, plain follow-ups, cancellation, budget exhaustion, isolated sessions and pause-before-write. Boundary tests cover unauthenticated, wrong-role, cross-tenant, forged reference, stale approval and replay attempts as applicable.

Run the same audio and inventory fixtures against the baseline and replacement with the same provider/model settings. Include baby clothes with competing exact-title and tag-only matches, chemicals with irrelevant similarly named objects, existing-item moves versus additional-item creation, nested destinations, uncertainty, zero results, corrections and follow-ups. Preserve provider/tool traces safely and review full conversations. A fluent but irrelevant answer fails.

Measure STT, each model/tool round, answer, first playable audio, end-to-end duration, provider calls, tool calls and request/response bytes where observable. Use at least ten paired runs for the central read scenarios; report median and p95 plus failures and sample size. Do not exclude failures to make latency look better. Compare like-for-like fixture runs separately from production networking and physical-device trials. No material regression in useful-answer latency or accuracy is acceptable without explicit user review; seek improvement by eliminating redundant model stages and reads. Do not hard-code fixture answers or select a different model merely to pass.

All builds and compiled tests run in CI. Live ADC evaluations use the authorized host and configured Google credentials without exporting secrets. Require code-critic review, green regression/security checks, deployed conversation evidence, device verification with the user, healthy GitOps rollout and TestFlight availability before declaring the overall goal complete. Configuration expansion remains deferred until the voice milestone succeeds.

## Native provider continuation

The Google adapter uses native function declarations and function call/response messages. Preserve the original ordered assistant parts, including thought signatures, as bounded opaque provider continuation bytes on the project-owned message. The loop copies those bytes unchanged; only the matching adapter interprets them. Exclude this state from ordinary JSON diagnostics, speech and presentation. Never reconstruct or concatenate signed parts. Tool results must follow the complete assistant call batch and retain correlation. This follows Google's [function-calling guidance](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/multimodal/function-calling) and [thought-signature contract](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/thought-signatures).

Native textual answers may finish the loop directly. An optional presentation function supplies spoken/display text and authorized reference IDs when cards are useful; it is not a mandatory reasoning stage or semantic classifier. Request retries remain within the application's shared budget and deadline; the adapter must not conceal extra inference attempts.

The native Google conversation adapter bounds each response body to 1 MiB and each retained assistant continuation to 256 KiB, rejecting oversized input continuation before a network request. These are protocol safety ceilings, not semantic restrictions. A candidate must report successful `STOP` termination; partial text from token exhaustion or another termination is not a completed answer. Keep errors safe and preserve the caller's context for recovery. The native adapter uses the existing zero-temperature default so baseline comparisons do not silently change sampling.

## Application integration acceptance

A native conversation session requires configured STT, conversation model and TTS providers; it must not require a legacy investigation or wording provider. Resolve and authorize the session before invoking any provider. At the real WebSocket boundary, verify legitimate sessions complete and unauthenticated, malformed-token, outsider, cross-tenant and wrong-inventory attempts cannot invoke the model. Resource cards may be selected only from scoped observed records and need not repeat their titles in display prose. Preserve legitimate inventory titles even when they resemble implementation vocabulary. Ordinary read outages become safe tool feedback that can support another model-directed attempt; do not expose raw repository/provider error details. Auth failures and cancellation stop the loop.

## Model-authored proposals

The proposal tool accepts a bounded summary, risks and ordered domain command objects (ID, command kind, summary, typed arguments). It has no approval, execution, principal or scope arguments. Existing resource references must come from scoped tool evidence and be reauthorized before preparing review; dependent command references must refer to valid earlier commands. Creating a proposal never changes inventory, and later tool calls in the same model batch must not execute after it. Persisted proposal IDs are application-owned. A read-only session cannot propose a write.

Keep domain command type/argument validation and dependency validation. Do not ban ordinary inventory titles or descriptions because their text resembles provider or credential terminology; unexpected fields are rejected by typed argument validation. Only explicit approval through the existing authorized decision boundary may execute the plan.

Resolve fallible review metadata before saving a proposal. A retryable read failure must leave no draft behind; successful persistence must immediately pause the loop. Explicit valid command IDs keep dependent review and execution references stable.

## Active conversation continuity

An active voice session retains project-owned messages, tool results, authorized reference metadata and opaque native provider continuation between utterances. Keep this state private to the server-side session, scoped to its original principal, tenant, inventory and session ID; never accept it from client payloads or expose it in events. Serialize turns on that session and reject scope changes. A new session starts empty. Session closure releases this state; reconnect persistence is not required for this milestone. Retain partial completed tool history after a failed turn so the next turn does not invent a result or repeat a change blindly. Reauthorize references before new mutations and validate current access on every utterance. Bound retained context by configurable limits without silently dropping native call/response pairs.

The operator sets `STUFF_STASH_CONVERSATION_MAX_CONTEXT_BYTES` (default 2 MiB, positive integer). Count serialized messages plus opaque provider bytes before each inference and when retaining the next context. Exhaustion explicitly ends that context and requires a new session; never silently truncate native messages. A plan already prepared for review must still be shown even when retaining its history reaches the cap.

## Existing workflow migration

For a stored staged workflow, the interpretation step's explicit provider becomes the conversation model; an empty reference uses the tenant's configured language profile. Preserve that profile's prompt and the interpretation step's user-authored guidance. Assessment and response stages do not execute or require providers, and grounded/template response settings do not replace the model's answer. Convert stored stage definitions at the persistence boundary into a single provider reference, instructions and bounded tool-call, model-call, elapsed-time and follow-up settings. New revisions expose only those settings. Model-call, tool-call and processing-time budgets apply independently to each utterance; pauses between utterances consume none of the next turn's allowance. Follow-up and retained-context limits apply to the session. Preserve historical revision identity and show the migration notice. Historical quality evidence cannot certify the new loop.

Native inventory search makes one fuzzy search call with the complete query; it does not stop after an exact-name match or apply the retired staged-workflow retrieval policy. Search may match terms across titles and tags, leaving the model to judge relevance. Live fixture traces record bounded native function calls/results and request/response sizes and timing, excluding thought text, signatures, credentials, headers and audio payloads.

The optional `present_answer` tool finishes a turn with model-authored `spoken`, `display` and up to 16 `assetIds` from observed authorized reads. It does not execute inventory changes or require another inference call. The server validates reference membership and text size/encoding, constructs cards from trusted metadata, and stops later calls in the batch. Native call/response history remains paired for follow-ups. Plain-text answers remain valid without using this tool.

The conversation instructions explain that text alone cannot display item cards and direct the model to use `present_answer` for answers about retrieved items. This is presentation guidance, not a server-side intent gate or an extra response-generation stage.

Tool definitions identify a response tool that can deliver any natural answer, including a question or an answer without cards. For a catalog with a response tool, the Google adapter uses one schema-constrained JSON tool-call envelope per inference. Its schema binds each tool name to its declared argument shape, including the answer tool; the model chooses any combination of reads, a proposal, or its completed answer. Tool results return as correlated JSON messages, and opaque assistant content is preserved. No separate classification or response-writing stage is introduced. Without a response tool, native automatic text/function calling remains supported by the same provider port. The core accepts plain text from other providers. The response-tool catalog remains stable for an active session.

This adapter choice follows observed live failures with Gemini 2.5 Flash Lite: automatic function mode omitted answer cards, while forced function mode added repetitive reads and inaccurate location prose. Google documents [structured output](https://ai.google.dev/gemini-api/docs/generate-content/structured-output) separately from native function calling; combining both protocols is not assumed for older models. The JSON envelope uses ordinary conversation roles without native function declarations. It requires at least one tool call, assigns application-owned call identities, preserves tool/result pairing and returns the same project-owned tool calls used by the native adapter. Protocol constraints must never choose the user's semantic operation.

Text evaluation preserves the native conversation interface for both default and explicit workflow providers and counts every actual conversation call, including failed calls. Its speech substitutions do not change the language loop. Outcome projection validates trusted card identities and metadata independently of prose; a card's recorded parent context can demonstrate displayed location without requiring a separate parent card. These structural observations do not certify the factual meaning of model prose; realistic trace review remains required.

## Recorded chemicals regression

The recorded-audio HTTP boundary corpus includes “Do I have any chemicals?” with tagged acetone and sodium carbonate in a garage cabinet, competing chemistry books and protective equipment, and an unrelated household item. A successful answer must select the actual chemical records, exclude the books/equipment/unrelated item from result cards, finish without a proposal, and produce speech. Trace review must additionally establish that the model gives a natural, relevant answer without inventing properties, quantities or safety claims. Keep the shared transport/audio runner identical across baseline and candidate fixtures; no fixture-specific production instructions are permitted.

The audio runner requires separate `STUFF_STASH_VOICE_BABY_CLOTHES_AUDIO_FILE` and `STUFF_STASH_VOICE_CHEMICALS_AUDIO_FILE` paths for their respective scenarios so a full enabled corpus cannot silently reuse the wrong recording.

Session preparation must reject a provider set that supplies only the retired inference/wording interfaces. It must not start a session, transcribe audio, or silently enter the old investigation path. Selected historical workflows likewise require their chosen interpretation provider to support the conversation port; incompatible providers fail preparation before capture. This is a capability check, not an intent classifier or restriction on model-authored requests.

Relevance guidance distinguishes a lexical search hit from evidence that an object satisfies the request. The model must assess the object itself using its title, tags and description, distinguish members of a requested category from merely related objects, and choose cards to support its answer. Search match fields explain retrieval, not category membership. These are general model instructions; no category-specific filters, examples, prose vetoes or extra classification calls are introduced.

## Intermediate live evidence — 2026-09-05

Ten predeclared alternating-order pairs used the same recorded baby-clothes question, isolated fixture, ADC host, Gemini 2.5 Flash Lite and Google Standard-C TTS. Baseline `26434c69d` contains v0.17.0 production code with the matched measurement fixture; candidate `71a5e8753` uses the model-led JSON tool envelope. These are intermediate results, not release acceptance or physical-device evidence.

| Observation | Baseline | Candidate |
| --- | ---: | ---: |
| Successful complete audio outcomes | 8/10 | 9/10 |
| Median first audio | 5,675.5 ms (10 observed) | 4,444 ms (9 observed) |
| Nearest-rank p95 first audio | 12,336 ms | 11,839 ms |
| Median terminal event, including failures | 5,676.5 ms | 4,496 ms |
| Median inference calls | 3 | 2 |
| Median measured request bytes | 124,257 | 108,212 |

Baseline runs 1 and 9 spoke a generic failure instead of an answer. Candidate run 2 failed speech recognition after 10,242 ms, before any logged inference HTTP call, with no answer/audio; its missing audio latency is not treated as zero. The nine candidate answers included all three clothing cards and both bins, and their traces used one search plus presentation. Both versions had a slow successful outlier. First-audio medians include baseline failure speech; they are not medians of useful answers alone. Request bytes measure logged STT and language request bodies, excluding OAuth, TTS and transport overhead; a failed unlogged request is not proof that no bytes were sent. Ten observations are insufficient to claim stable tail latency or general accuracy.

The next recorded chemicals preflight (`654c9350f`) failed relevance despite completing speech in 4,347 ms: both chemical records were found, but the model also included a reference book and protective gloves as chemical results. The matched v0.17.0 preflight (`fc4add58a`) failed the same relevance assertions at 5,724 ms. These failures motivated general relevance guidance and must remain in the evidence; chemistry and broader interactions have not yet met release acceptance.

Answer presentation must stay focused on the user's request. Its tool/argument descriptions tell the model that authorized search results are a candidate pool, not an instruction to attach every result. Related but unrequested objects should be omitted from prose and cards. Card selection remains the model's judgment; the server checks only authorization, observed references and presentation shape.

The first removal pass deletes the application investigation loop, semantic resolution policies, transcript phrase gates, deterministic completion/recovery, and generated-prose acceptance rules. Existing general search/detail/list/checkout/history tools, final presentation validation, safe diagnostics and approval services remain reusable. Replace obsolete choreography tests with the conversation outcome suite; preserve their shared inventory fixtures and retain presentation-boundary and speech-error tests. Security tests at the HTTP boundary remain required throughout migration.

The model's tool catalog must include the preserved authorized audit-history, checked-out inventory and checkout-history reads. Their definitions expose the existing bounded arguments, and the model selects when to use them. History reads require a previously observed asset ID and retain normal access checks; checked-out inventory remains scoped to the active inventory. Verify seeded history/checkout facts reach the model and its natural answer through a real conversation session, as well as adversarial HTTP coverage.

Prepared workflows contain only the pinned conversation provider and its budgets. Remove the retired per-stage model binding/execution and response-generation implementations; historical stage data is used only to recover the chosen provider and useful guidance during migration. An incompatible provider fails preparation rather than installing an alternate engine.

A failed conversation-model invocation retains the safe `language_inference_failed` stage code in session outcomes. Keep original error identity for cancellation, authorization and configured budget exhaustion; tool failures must not be mislabeled as model failures. Provider details never appear in user-facing errors.

Text evaluation runs the same conversation provider and tool loop as voice, substituting only transcription and speech output. Remove evaluation wrappers and test scripts for retired intent/wording phases; explicit evaluation providers that cannot converse are rejected. A model-authored proposal is captured for evaluation without approval or inventory mutation. Cancellation and real provider failures remain distinguishable from invalid model output.

Opt-in developer diagnostics preserve safe model-failure attribution for the conversation loop: report the conversation stage, model/tool call counts, authorized tool names and an allowlisted provider error category before the terminal failure. Never restore investigation phases, semantic evidence-round counters, raw provider messages or conversation dumps. Normal sessions and authorization failures do not emit this diagnostic.

All direct application and bootstrap injection accepts the conversation model port together with STT and TTS. Remove the separate response-generator injection and do not require retired inference methods for a native model. Environment-configured Google providers and the explicitly enabled development fake must populate that same conversation slot. The development fake retains its fixed tools demonstration, reads returned identities and parent location, and never serves as a fallback for configured real providers.

Configured profile factories, explicit workflow bindings and pinned evaluation providers expose only `ConversationModel`. Remove legacy inference/wording fields from provider sets and their runtime type-assertion fallback. A missing conversation provider fails preparation. Profile capability identifiers, credential pins, tenant scoping, prompt guidance, readiness probes and budget accounting remain unchanged. Deleted interface states are enforced by compilation rather than maintained through rejecting compatibility implementations.

Delete the unused Google investigation and separate wording adapters, their semantic prompt sanitizers, hidden retry machinery, and retired request/response DTOs. Provider authentication, safe status/timeout handling and readiness probes are verified through the native adapter. Native HTTP voice corpora replace the retired single-investigation live probe. One model invocation makes one HTTP inference request, including when the provider returns a retryable status.

Remove the orphaned domain investigation and response-brief schemas, creation-intent rules and deterministic response renderer. Their request classifications, semantic references, evidence phases and wording rules no longer represent runtime concepts. Preserve bounded inventory tags and vocabulary validation independently, and retain typed change operations and detail limits for evaluation command assertions. Historical workflow evidence limits remain isolated in workflow settings until settings migration; they do not authorize a runtime evidence phase. Existing presentation, proposal and boundary suites cover the replacement behavior; retired schema choreography tests are removed.

Inventory vocabulary remains available through an on-demand `get_inventory_vocabulary` conversation read tool. With no arguments it returns the active inventory's bounded custom asset types, custom-field summaries and tags, including truncation flags. Optional `definitions` requests (at most 12 distinct kind/key pairs) return bounded full definitions using stable keys, including enum choices and applicability. The model decides when this information is useful; do not preload it or add a required vocabulary stage. The tool accepts no scope, identity, or provider arguments. Recheck session access before the read and before exposing results to the provider; cross-tenant and sibling-inventory vocabulary, archived definitions and internal definition identities remain inaccessible. Invalid or unavailable keys are recoverable tool errors. Remove the unused cross-phase vocabulary merge machinery. Verify useful discovery and targeted definitions through the real WebSocket boundary, including viewer access, forged scope arguments, hidden keys, and revocation between model invocation and tool execution.

A diagnostic provider comparison on candidate `8274c9db6` used the same recorded chemicals question, isolated inventory and Google Standard-C TTS. Three Flash Lite trials failed the card-relevance assertion by including protective gloves; three Flash trials passed and spoke only the two chemical names and their recorded cabinet/garage location. All six transcriptions were correct and all used one search followed by model-authored presentation, with no proposals. First-audio observations were 4,731/3,807/4,009 ms for Flash Lite and 6,128/5,948/5,894 ms for Flash. Actual tool and answer traces support these scenario verdicts. This is a small diagnostic sample, not a release benchmark: changing the model affected both STT and inference, and production provider configuration was unchanged. The result motivates evaluating model capability and latency without adding category-specific application rules or relaxing relevance assertions.

Vocabulary reads use the existing custom-type, custom-field and tag application query services with conversation audit source. Do not reuse the retired loader's direct repository reads, which omit normal read audit and application observability. Project only the scoped query results into the provider vocabulary; preserve their pagination/truncation flags, and recheck access after loading before returning content.

Evaluation runs pin an explicit runtime contract (`conversation-tools-v1`) when queued. Persist that marker independently of workflow/provider/case revisions: matching those revisions cannot make a result from the retired runtime valid for this loop. Restore unversioned historical runs as legacy evidence; they remain readable, but cannot activate a workflow. Unknown runtime versions fail restoration. A queued or retryable expired in-progress run from a different runtime fails with configuration-changed before provider resolution or inference; it must not append new-runtime outcomes to old evidence. New runs always use the current contract, and repository updates preserve it as part of immutable run input. Verify successful current evidence still activates, while an otherwise identical restored unversioned run is rejected through the HTTP activation boundary.

An expired run that has already exhausted all worker attempts retains its existing worker-lost terminal failure, regardless of runtime contract. This preserves the recorded lease-failure cause and never permits provider resolution, execution or activation. A live lease remains owned by its current worker until expiry.

## Workflow settings migration

Active workflow definitions contain only `name`, optional `providerProfileId`, `instructions`, and `budget` (`modelCalls`, `toolCalls`, `elapsedSeconds`, `followUpTurns`). Model and tool limits bound one user turn; elapsed time bounds processing that turn, excluding the pause while the user speaks or considers a follow-up. Follow-up turns retain the session limit. The selected model receives general instructions; there are no interpret/assess/respond stages, retrieval modes, response modes or per-stage attempt settings. New API writes reject these retired fields rather than silently accepting ineffective configuration. Operator tool-call limits use `STUFF_STASH_WORKFLOW_MAX_TOOL_CALLS`; evidence-round and step-attempt settings are retired, with actionable rejection if configured.

The persistence adapter converts historical workflow JSON into this one active definition. Preserve name, the interpretation-stage model selection and general instructions, model-call/time/follow-up limits; discard obsolete assessment/wording directives and routing. Assign six tool calls, bounded by the current model-call allowance, for historical definitions; evidence rounds are not tool calls and must not be renamed into them. Expose revision `settingsMigration: "legacy-investigation-v1"` metadata so users know the old stage settings no longer execute. A newly saved revision uses the current contract and clears that historical notice. Do not mutate immutable stored history during reads or reintroduce a legacy executor. Historical evaluation documents use the same conversion while retaining their legacy runtime marker, so converted workflow settings never validate old quality evidence.

Current evaluation runs pin one conversation provider, without stage names or separate wording bindings. Historical provider fingerprints remain historical data and cannot execute because their runtime marker is invalid for this engine. The UI edits the single model and guidance plus actual budgets, and does not advertise retired modes. Use the existing generated API contract, frontend ports/adapters, revision conflict handling and tenant configuration authorization. Verify a flat definition round-trips at the HTTP boundary, retired fields are rejected, unauthorized writers remain rejected, and historical persisted revisions convert without changing their identities or author/time metadata.

The API settings migration is verified independently before client regeneration and turn-budget wiring. Test-first revision `a1334659b` failed CI `34011118788` on the intended boundaries: historical storage still exposed staged settings, and a valid flat HTTP definition was rejected. Preserve historical evaluation provider fingerprints without executable stage names, and test both legacy restoration and re-encoding so conversion cannot erase the runtime marker or migration notice.

Turn-budget regression coverage must exercise repeated public voice queries in one session with a one-call allowance per utterance: each ordinary model-authored answer succeeds and retains a processing deadline, while permitted follow-ups remain available. The client editor must expose one model selector, one guidance field and tool-call limits, with no retired stage, attempt, retrieval or response controls.

A processing timeout must persist the terminal session failure using a bounded cleanup context independent of the expired processing deadline. Cleanup does not resume model/tool work or execute proposals. Test this with a context-respecting session repository and a provider interrupted by the deadline.

CI `34011408250` passed all required checks for the flat API implementation (`20450691e`); its only failure was expected generated-contract drift. Client artifacts are taken from that CI generation. Test-first `29e59502a` failed CI `34011671089` because a completed one-call turn disabled permitted follow-ups. Preserve the existing pinned-revision integration test that independently proves a one-call workflow prevents a second model invocation, and cover configured tool caps as well as budget reset.

The WebSocket workflow authorization matrix must retain its authorized explicit-model case while testing the new budget semantics: complete two search-and-answer turns with a two-call allowance per turn and one allowed follow-up, advertise the first follow-up, then close normally after the session turn limit. Do not retain assertions that one completed turn exhausts a shared model allowance.

Post-migration diagnostic voice runs used CI executable `4635d83ea` with recorded input, real ADC providers, an isolated authenticated WebSocket inventory and actual TTS. Baby clothes with Flash Lite passed: correct transcription, one `baby clothes` search, all three clothing cards, both bins and Hall Closet in the spoken answer, first audio 6,414 ms. Chemicals with Flash passed: one `chemical` search, only sodium carbonate and acetone, correct cabinet/garage answer, first audio 5,994 ms. Chemicals with Flash Lite failed relevance again by including protective gloves (first audio 5,250 ms). Each used two inference calls after transcription. These single diagnostic trials do not replace repeated benchmarks, broader interaction coverage or deployed/device verification; production provider settings were unchanged. Desktop and mobile CI browser screenshots confirm the flat editor controls, and the browser save/evaluation/activation and revoked-access journeys passed.

## Extended live interaction corpus

Use isolated inventories and the authenticated WebSocket boundary with real configured STT, conversation and TTS adapters. Additional controlled utterances may be synthesized and converted to the supported mono MP4 input format; label this evidence as synthesized-audio integration, not microphone or device evidence. Audio inputs live under an explicit `STUFF_STASH_VOICE_INTERACTION_AUDIO_DIR`; enabled tests fail when required recordings are missing. Preserve provider traces and per-turn events for human review, and never log ADC material.

Cover moving an existing cordless drill to Garage without creating a duplicate; adding an explicitly additional drill; creating a blue toolbox in Garage and moving the existing drill into that new parent; asking which of two screwdrivers to move and resolving “the one in the kitchen” on the next turn; and recalling the located drill when asked “What color is it?”. Seed the drill as red in Office and the two screwdrivers in Office and Kitchen. Read/clarification turns must produce natural speech and no proposal. Mutation requests must propose exact useful commands with observed targets and destinations. All assets must remain unchanged before approval. Verify the stored proposal through the scoped repository port; a proposal is not execution evidence. Repeated live runs and trace review determine quality, without category-specific production rules or relaxed assertions.

## Proposal tool contract accuracy

The provider-visible proposal schema must describe each supported domain command's own argument shape, with command kind as its discriminator. Creation declares title, kind, description and optional existing or earlier-created parent; it must not advertise an `assetId` for a new identity. Move declares an existing `assetId` and optional destination; lifecycle commands declare only the existing target, and checkout/return may also carry details. Explain that `parentCommandId` references an earlier create command and cannot substitute for an existing `assetId`. Creation can set its parent directly, without a later move of a nonexistent ID. Keep required fields and additional-field rejection aligned with domain validation. This is output-contract validation, not classification of user requests or a prescribed reasoning sequence.

Verify the public tool schema against the supported argument families and preserve proposal/approval authorization tests. Re-run the recorded creation and dependency failures with real ADC providers; do not count a more restrictive schema alone as proof of useful conversation quality.

## Google constrained-decoding array limits

The full typed proposal catalog at `e23f003b3` triggers Google's schema-complexity HTTP400 before inference. An isolated real ADC comparison confirms that the same discriminated command shapes succeed when the provider-side `maxItems` is omitted from an array of alternatives; reducing that maximum also succeeds but would narrow supported proposals. Google documents array limits combined with optional fields and enums as sources of schema complexity in its [structured-output guidance](https://cloud.google.com/vertex-ai/generative-ai/docs/multimodal/control-generated-output).

The Google JSON-envelope adapter therefore omits `maxItems` only on arrays whose item schema directly contains `anyOf` alternatives, preserving that maximum as a field description. It preserves all command discriminators, argument properties, required fields, additional-field rejection and other bounds. Walk schema nodes rather than arbitrary object keys, so a property literally named `maxItems` remains valid. Never mutate the project-owned tool catalog or apply the transformation to ordinary arrays. Application tool/domain limits remain authoritative: oversized proposals are rejected before persistence and no additional commands can execute. This provider compatibility translation cannot select commands, add model retries or restore the old executor.

## Search coverage guidance

The `a5d31b9db` recorded corpus passes all five interaction cases with Flash. Flash Lite passes creation, dependent moves and follow-ups but proposes one of two screwdrivers after requesting `limit: 1` and receiving `hasMore: true`. The search tool must explain that its optional limit defaults to 20, a low limit can conceal competing matches, and `hasMore` means the returned candidates cannot establish uniqueness. The model can raise the limit, narrow its search or ask a question; the server must not classify ambiguity or automatically reject a proposal merely because a prior search was truncated. Use the existing real-audio ambiguity failure as the regression case, then repeat both model trials without weakening the expected question and selected target.

## Recorded acceptance and matched performance, September 6, 2026

Candidate `9add6bf5d` passed the complete CI suite (manual run `34014082604`) and three consecutive real ADC runs of all five recorded interaction cases with Gemini 2.5 Flash: 15/15 scenario passes. Review of actual speech and typed proposals confirms existing-item moves, additional-item creation, dependent toolbox creation/move, an Office-versus-Kitchen screwdriver question followed by the correct Kitchen move, and location/color continuity. These tests pause at proposal review and never approve inventory mutations. They use controlled inventory and synthetic recorded audio through the authenticated WebSocket, not a physical device.

Three recorded chemicals trials with the same candidate and Flash passed. All spoke “Yes, you have Sodium carbonate and Acetone. Both are in Cabinet A in the Garage.” Cards referenced those two chemical records only. First audio arrived at 4,784/5,160/5,301 ms. The book, protective gloves and unrelated household object were excluded by the model; no category filter or factual response template ran.

Ten alternating-order baby-clothes pairs compared candidate `9add6bf5d` with the CI-built v0.17.0 measurement baseline `fc4add58a`. Both used the same recording, fixture, ADC host, Gemini 2.5 Flash for transcription/inference and Google Standard-C TTS. Candidate passed 10/10; baseline passed 9/10, with its sixth trial failing transcription at 10,211 ms. Every successful candidate answer identified all three clothes records and the two recorded bins in Hall Closet.

| Measurement | v0.17.0 baseline | Model-led candidate |
| --- | ---: | ---: |
| Median first audio, observed successful trials | 11,161 ms (9 trials) | 5,834.5 ms (10 trials) |
| Slowest observed successful first audio | 14,722 ms | 7,245 ms |
| Median model calls | 3 | 2 |
| Median measured request bytes | 124,585 | 122,780 |
| Median measured response bytes | 5,636 | 3,369 |

Request/response byte measurements cover successful traced transcription and inference HTTP bodies, excluding OAuth, TTS traffic and transport overhead. Missing I/O during the failed baseline transcription is not proof of zero network use. These small samples demonstrate improvement for this workload; they do not establish universal latency or reliability. Keep failed trials in the evidence. Metrics and safe traces are retained with the controlled evaluation artifacts; do not merge the baseline measurement branch.

Flash Lite remains a quality limitation: at `9add6bf5d`, it passed existing moves, additional creation and color follow-ups but invented references repeatedly for a dependent move and selected one of two visible screwdrivers without asking. Application validation rejected invented IDs, and all proposals remained subject to approval. Do not claim that the same quality has been established for smaller/local models. Production model configuration has not been silently changed. Physical device acceptance, TestFlight availability and the GitOps deployment are separate release requirements.

## Category retrieval without matching vocabulary

A category request can describe belongings whose titles and tags use different language. Live acceptance must separately cover an empty literal search, rather than only filtering candidates that already share the category word. For “Do I have any chemicals?”, seed acetone under `cleaning` and silicone sealant under `adhesives`, with no `chemical` term in titles, descriptions or tags. Seed a cleaning brush and caulk caps as related accessories to exclude. The model should discover relevant vocabulary or otherwise broaden its search, return the two substances with recorded locations, and exclude accessories. Do not prescribe a fixed tool sequence or add category classification, deterministic expansion or a response fallback. Run this regression through the existing real-audio authenticated WebSocket harness with both Flash and Flash Lite; inspect tool traces, actual speech, cards and first-audio latency. A successful literal-tag fixture does not prove this scenario.

The deployed v0.19.0 Flash Lite check exposed the gap: after an empty search it said it found no chemicals, although authorized inventory reads showed spray paint and sealants under related tags. This is a quality failure, despite successful audio delivery. Keep this result separate from the successful deployed baby-clothes answer and from isolated fixture results.

Initial CI-built live runs of this regression failed with both Flash Lite and Flash: each searched `chemical` once and answered with no matches. The shared model instructions must explain the difference between lexical retrieval and the user's conceptual question. An empty term search is not evidence of absence; the model can inspect vocabulary, consider related concepts, or inspect an appropriately scoped inventory list and assess relevance. It must not substitute “nothing labeled with that word” for answering the user's question. This is provider-neutral model guidance, not an application-enforced step sequence, answer veto or deterministic retry. Preserve concise uncertainty when useful investigation still leaves incomplete evidence.
