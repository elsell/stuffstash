pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
    go-test:
      glob: "*.go"
      run: GOCACHE="$PWD/.cache/go-build" go test ./apps/api/...
    go-structural-rules:
      glob: "*.go"
      run: scripts/check-go-structural-rules.sh {staged_files}
