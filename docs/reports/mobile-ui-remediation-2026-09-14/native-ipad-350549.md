# iPad native run35054957505

Job104663309474 at source8a256a2a completed63/73 passing,10 failures. Artifact
10432370210 retains the result; `/tmp/native350549-ipad.log` is the downloaded job
log. This report classifies log evidence, not an inspection of every screenshot.

Normal-size failures: the retained hidden-header Add comparison does not reach
its name field; color activation does not expose Sliders; the separate color
target assertion reports36points; controlled address and no-accessory ordinary
input lose/reorder text; Home's geometry assertion reports36points; voice-location
search fails. Enlarged-text Edit metadata, Edit tag disclosure and Move-here also
fail. Keep those three for the later enlarged-text pass.

Sharing, Place search, static search placement, the configured-header Add journey
and other Add typing journeys pass in this run. Results vary by platform and run;
these passes do not erase earlier failures or establish physical-device behavior.

The Add hidden-header comparison preserves the old configuration. Production
`src/app/_layout.tsx` already declares Add's native header before presentation;
the configured-header journey is the matching entry configuration. Keep the
negative comparison visible without treating it as evidence that the currently
configured production route remains loading.

Home logs identify Add as the first failing36-point frame; Notifications was not
reached by that assertion loop. Separate notification center/edge probes pass,
but cannot establish Add/Profile hit behavior. Runner-only destination probes now
exercise their real callbacks while retaining the original geometry test. Native
execution is pending; no touch-target acceptance rule has been weakened.
