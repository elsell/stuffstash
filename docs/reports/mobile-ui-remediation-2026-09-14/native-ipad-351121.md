# iPad native audit — run35112198520

Source1c2f8173, merge7b5627db6ebf613330b48bc20430ae3ad5d804c7;
iPad mini (A17 Pro), job104851680554. Complete XCTest log:77/84 pass,
7 failures,3501.858 seconds. Terminal job failure. Onboarding3/3 passes separately.
The iPhone fixture job subsequently completed65/84; see native-phone-351121.md.
Complete log is `/tmp/native351121-ipad-complete.log` locally. Artifact10458445165
is1,307,623,702 bytes and is retained as `/tmp/native351121-ipad.zip` on paul.
Selected attachments are `/tmp/ipad351121-selected` on both hosts.

## Normal-text failures and comparisons

- Ordinary system color-picker activation fails to open. The single accessible
  name, nine delivered-touch probes and separate target-opening journey pass.
  This retains M51's inconsistent activation finding; passing probes do not erase
  the failed ordinary workflow or establish a cause.
- Controlled address diagnostic produces `hle.invalid`; controlled name without
  accessory produces `Nativ draft namee`. Exact input is not accepted. Unassisted
  controlled-name, ordinary controlled-name, ordinary multiline, seeded address,
  native-default-assisted, system and uncontrolled address comparisons pass.
- Voice location fails the older assertion requiring a still-expanded search field
  after Clear. The newer22e059cf iPad acceptance adjustment is not in this source;
  that candidate requires restored results and usable in-header search/re-entry.
  This failed run cannot verify that later adjustment.

Normal Move Here rejection/retry now passes with the corrected candidate selector.
Sharing creation/recovery passes using the older menu interaction, before the newer
direct-cancellation presentation. Place, preconfigured Place, managed registration,
static search comparison and Settings collection search all pass on this iPad.
All Add presentations, draft photo paging/removal, Home actions/tab return, inventory
switching, native Settings save/discard/archive and provider recovery pass.
Do not promote these results to phone acceptance or to the newer queued source.

## Enlarged text and release status

Edit metadata, Edit tags and Move Here fail at enlarged text. Asset-region recovery,
command-height comparison, Details commands and footer appearance pass their
respective enlarged-text journeys here. Normal-text issues remain the priority.

The run is failed. Current inbox/menu/Sharing changes are newer and require their
own native acceptance; Linux source checks do not substitute for it. The newer35121454700 native run has started normally; preserve its jobs.

## Inspected failure evidence

The ordinary color button reports hittable at(684,326.5),36×36 points before tapping;
the final [capture](evidence/ipad351121-color-unopened.png) remains the unchanged
parent with no system picker. This confirms failed activation, not just a missing
Sliders selector. Both controlled-input final hierarchies retain their incorrect
values (`hle.invalid` and `Nativ draft namee`), including the mirrored observed-value
text. They are not merely immediate incomplete-value reads. The voice-location
final hierarchy again exposes an in-header Search button and restored choices,
consistent with the separate static comparison behind the newer acceptance rule;
the newer full re-entry assertions still need their own execution.

## Artifact retention update — September 16

The full archive named above was removed from paul after confirming its GitHub
artifact remains available through September 30. Selected captures, hierarchies,
and recorded findings remain retained. This cleanup recovered space without
removing current-run evidence or touching active native jobs.
