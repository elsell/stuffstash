# iPad native results — run35131892834

Source dfad17ab; tested merge21939a989d89e3f0e3a4bf10133f3dab63c8b073.
iPad mini job104914725594 completed86 tests in3940.788 seconds:79 passed,
seven failed. Unlike run351214, result export and artifact upload completed under
the120-minute job budget. This is preserved evidence, not release acceptance.

Three normal-size text-entry cases still corrupt the expected Native draft name:
controlled without accessory becomes `Native aft namedr`; ordinary controlled
becomes `Native dreaft nam`; seeded without accessory becomes `N draft nameative`.
The later trace candidate is required to inspect the native/React event sequence.

Home return error recovery fails its five-second full-message visibility predicate.
The retained final screenshot `33436A0A-6034-4106-B6CC-B49E9099B467.png`, inspected
visually, later shows the complete two-line error below the sheet header, retained
Returned clean text and reachable Cancel return/Save commands. Hierarchy
`480EB851-57A0-41CC-ACE5-538B5C90A69A.txt` places the header at(82,298.5),580×54
and the error at(102,409),540×33.5. Those later bounds satisfy the intended geometry.
This does not prove the predicate was true during its earlier wait or verify the
unexecuted retry and dismissal. The failure needs transition/observation diagnosis;
there is no confirmed clipping defect in the retained final state.

The other three failures explicitly exercise enlarged text: Edit metadata recovery,
Edit tag disclosure and Move Here recovery. They remain behind normal-size work.

Notification inbox navigation and all nine delivered read-control touch probes
pass. Add unfinished-tag collapse/reopen passes on this device before the later
bounded-wait correction. Ordinary color opening/clear, managed-search composition,
preconfigured Place search and Settings collection search/Add pass. These iPad
results do not clear the phone failures for the same interactions.

Log: `/tmp/native351318-ipad-complete.log` locally. Artifact10465250322 is
1,284,065,186 bytes and remains on paul at `/tmp/native351318-ipad.zip`.
Selected Home return attachments and manifest are under `/tmp/ipad351318-selected`
on both hosts. The new native run35140471580 at a1b827e0 is active, including
input-event tracing and bounded unfinished-tag transition observation. Earlier
pending run35132227322 did not execute jobs; no active run was cancelled.
