Inventory collection clearance — source risk, not confirmed defect
The retained /assets collection has a separate FlatList without automatic insets.
Home shortcuts now use Browse, but this destination remains routable. Review it
under actual production tabs/accessory before changing layout ownership.
One baseline native observation: populated24-item repository, real screen/query,
entry heading and final unique tag. Use full-screen entry/final captures and the
existing chrome geometry helper; mid-scroll content under chrome is not failure.
If final content clears, retain existing ownership. If not, fix only this consumer
and repeat the same acceptance. No Map or blanket inset changes are presumed.
Fixture route isolation failed before setup implementation;15 tests now pass.
TypeScript/structural checks pass. Critic caught an incorrect helper name before
dispatch; corrected to existing verifyFooterClearsPersistentChrome. Its failure
still retains a full-screen tearDown capture. No runtime result yet.
