SettingsActionRow now uses the existing bounded native command adapter. Visible
labels and descriptive accessible names are separate; destructive role/disabled
behavior preserved. Navigation and selection rows remain unchanged.
Shared consumers: Browse/Expiration reset; expiration date reset; reminder save
retry/discard; notification load/back/device-settings actions; Browse filter
load retry/cancel; voice profile action.
Regression: NativeCommandButton accessibility test failed before implementation
because iOS exposed Retry instead of Retry saving reminders. After implementation,
all2005 mobile tests passed, TypeScript and structural checks passed. Critic found
no source blocker; long-label wrapping/native composition remain acceptance work.
Android API36 Pixel6 normal text: build /tmp/settings-bounded-build.log succeeded
in54s. Reviewed /tmp/settings-bounded-filter.png shows bounded Reset all with no
clipping and separate footer. Native tree preserves Reset all filters description.
/tmp/settings-bounded-replay.log passes actual Type→Items→Reset all→All types.
This verifies the representative filter consumer, not all reminder states, narrow
screens, iOS or assistive traversal. Native iOS acceptance still required.
Narrow Android follow-through: normal text at840×1920/420dpi (320dp wide), same
installed build. Reviewed /tmp/settings-bounded-narrow-reset.png after scrolling:
Reset all fully visible above the fixed footer, with no horizontal clipping.
Actual Type→Items→Reset all→All types passed. System size restored to1080×2400.
This supplements the representative filter evidence; remaining consumer/iOS limits
above still apply.

Consumer composition review (source at bc3ea7d4): reset/date commands stay inside
their filter sections and remain secondary to Show results. Reminder Retry and
Discard change remain paired with the retained-draft error. Notification device
setup and Settings recovery stay adjacent to their switch/outcome. Voice setup
exposes one readiness-dependent command, separate from service selection. The
Browse load-error Retry/Cancel pair uses the same bounded adapter outside a group.
No navigation or value-selection row was converted into a command button.

Native acceptance still needs a representative paired recovery composition and
the longest visible device-settings label, in addition to the running iOS filter
check. Source review alone does not establish wrapping, touch or assistive behavior.
The installed Android fixture has no notification-settings route; no notification
runtime acceptance was inferred from an unavailable route or a different screen.

Android recovery acceptance: production ExpirationReminderEditor in the isolated
SettingsCommandFixture passed failed-save → Retry (saved Off), then failed Custom
save → Discard (restored Off). The longer Open device settings button activated
once. Reviewed /tmp/settings-command-failed-save.png and
/tmp/settings-command-recovered.png: both recovery commands and the longer label
are fully visible, bounded and separated at normal text on API 36 Pixel 6.
Build /tmp/settings-command-composition-build.log succeeded; replay
/tmp/settings-command-composition-replay.log passed. This closes the Android
composition gap above. It does not verify real device Settings launch, assistive
traversal or iOS composition. The new focused settings-commands native case retains
those same state assertions and capture points for the next iOS verification.

Divider alignment correction: the reviewed failed-save capture exposed the shared
56pt icon gutter on icon-free recovery rows. SettingsSeparator now defaults to the
16pt row inset; the Settings root and scoped icon-navigation groups explicitly
retain the derived icon gutter. Other separator consumers were inspected and have
no leading icon. Stacked rows use the ordinary inset. Before/after Android capture:
/tmp/settings-command-failed-save.png → /tmp/settings-divider-after.png. Reviewed
after image aligns the divider with command bounds. The full recovery replay still
passes after the rebuild; 73 Settings tests, TypeScript, structural checks and critic
review pass. iOS visual verification of this spacing correction remains pending.
