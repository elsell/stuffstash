Current release diagnosis, run35965781821 at995c0859:
- iPad persistent-tab workflow passes, including the final both-tabs hit-point gate.
- iPhone reaches the exact draft return but screenshot acquisition times out at
  line795 before the final gate. This is incomplete evidence, not a new touch bug.
- Settings save/reopen fails on both devices at3375: Back opens Discard changes
  despite no edit after reopening. Screenshots confirm the false dirty state.

Cause: native Name initialization republishes the loaded name. withEditorName
regenerated the hidden stable key even for an existing record whose display name
had changed. Correction preserves the saved key in edit mode; creation retains
automatic suggestions and manual-key ownership. A rendered regression failed on
unchanged-name replay before the correction and passes afterward, including a real
rename, reverting to the saved name, and clean Back.64 focused checks, TypeScript,
structural checks and critic review pass. Native exact create/readback and tab gates
remain required; the previous run stops before New Tag typing.

Artifacts: phone10795886346, iPad10794884384. Reviewed phone false-discard capture
/tmp/move-layout-review/settings-name-phone-testSettingsSaveReadsBackFromTheSameCollection-final.png.
Logs: /tmp/settings-name-{phone,ipad}.log.gz.

Earlier35964187818 atf5d7961b: phone passed; iPad touch roundtrip preserved the exact
draft but the final isHittable assertion failed. The new iPad result supersedes it.
isHittable measures computed hit points, not complete VoiceOver acceptance:
https://developer.apple.com/documentation/xcuiautomation/xcuielement/ishittable
Do not repeat coordinate-selector or header-overlap experiments from that old result.
