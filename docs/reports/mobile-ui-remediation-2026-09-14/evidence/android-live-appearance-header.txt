Current outcome (2026-09-24): scoped Android API36 Browse appearance acceptance
passes on candidate APK
dac0ad4f13e4b4117139c6a43593af80ef99e6f081244b2e1f3d9bcb732dc21e.
This is fixture/emulator evidence, not physical-device or whole-app certification.

Corrections:
- Route synchronization no longer depends on query-adapter identity. A mounted
  failing-then-passing test proves replacement retains Camping while real route
  query changes still apply. Native replay confirms the prior Map query loss fixed.
- Android segmented hosts recreate only on semantic palette changes; controlled
  selection/callbacks remain. iOS host identity is unchanged.
- Android native search gets semantic text/hint/tint/header-icon colors. No query
  reseed, screen remount, native dependency patch or provider removal is used.

Acceptance on the named clean APK:
- Map selected → dark → light retains visible List/Map and inventory content.
- Open Camping → dark → light retains query/results; append gear and clear pass.
- Reviewed dark Map/query images show visible controls and readable search text.
- Initial runner incorrectly expected one Android Back to close search; its capture
  shows only the keyboard dismissed. Focused clear → keyboard dismissal → native
  empty-search close restores toolbar; selecting List works. No production change
  or removed assertion was used to hide a close failure.
- Notifications Unread → dark → light retains selection and excludes read item;
  All restores Emergency batteries. Reviewed dark capture shows readable segments.
-26 focused source tests, TypeScript, structural checks and critic review pass.

Native evidence on paul and local copies:
/tmp/android-header-palette-build.log and android-header-palette-replay.log
/tmp/verify-live-appearance.py (final Back-only close expectation fails as above)
/tmp/verify-search-close.py and /tmp/verify-segmented-inbox.py (pass)
/tmp/header-dark-map.png, /tmp/header-dark-query.png, /tmp/header-close.png,
/tmp/inbox-dark.png (local reviewed captures).

Bounded diagnosis retained to prevent rediscovery:
Baseline1fa57a5f lost List/Map on live dark change and retained dark Search icons;
cold dark worked. Early palette key, JS reseed and native menu guard candidates
were not accepted because query retention failed. The native lifecycle trace then
showed stable fragment/header/search identities, no detach/stop/close, and query
length7→0 before final header/menu updates. List-only sequence retained query.
Traces android-header-lifecycle-events.json (29 events) and
android-header-map-lifecycle-events.json (38 events) contain identities/lengths only.
Source review and the mounted regression identified adapter-dependent route resets.
After correcting that confound, the integrated rendering correction above passes.
Do not repeat provider removal, query reseed or the rejected native menu guard.
Recorder was removed; all three original dependency hashes verified; clean APK
rebuilt/reinstalled before acceptance. No instrumentation enters production.

Remaining scope: iOS regression gate for the shared Browse route-state change,
other device/window/assistive configurations and the comprehensive audit remain
open. This follow-up is separate from already-merged release35976929170.
