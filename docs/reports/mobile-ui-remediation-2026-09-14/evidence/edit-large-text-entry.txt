Current diagnosis: broad35980094051 atc7c44b45 did not enter Edit for phone
metadata, phone tags, or iPad metadata. Reviewed complete final captures show the
oversized audit menu. These failures do not establish product layout defects.
iPad tags did enter Edit; New tag extends slightly below the scroll viewport.
Do not infer inability to scroll or an input problem from this one capture.
Artifacts10805146387(phone),10806201894(iPad); extracted only named cases.

Decision: use existing URL entry for these enlarged-text cases; retain all actual
editor assertions. Normal-size menu-driven cases remain unchanged. One focused
replay on phone/iPad distinguishes fixture entry from actual editor reachability.
If editor fails, use its complete captures to choose correction; if it passes,
close only these named scenarios. This is not a rerun of text-provider diagnostics.

Replay36087906732 at938f32b1: phone metadata recovery passes; iPad tag draft,
selection and cancellation passes. Phone tag workflow fails reveal(New tag):
scroll bounds bottom840, button y767.3 h77.3 bottom844.6, measured host y782 h48.
Final full screenshot confirms clipped native background. iPad metadata reaches
Retry tags and dispatches a tap but button remains; no common-cause claim yet.
Artifact10844938360(phone),10844019839(iPad). Exact failure lines1557 and1812.

Implementation decision: remove outer fixed-size/frame proposal from shared iOS
command, use large native control sizing and flexible label measurement. Preserve
quiet-action minimum target, current/disabled callbacks and roles. Repeat both
large Edit tests plus ordinary Detail/Sharing/reminder shared consumers. No font
scaling cap, guessed bottom padding or provider experiment.

Source checks:59 focused tests across native command semantics, Detail, Sharing
and reminders pass, plus TypeScript/structural checks. Critic found no source
blocker; ordinary size and enlarged measurement remain native acceptance gates.
Representative consumers use flexible wrappers (AssetTagSelectionField,
SettingsCommandRow and Sharing command groups); no per-consumer padding added.

Visual acceptance decision (2026-09-25): REJECT b25867d9. Completed native run
36090229503 passed5/5 phone and5/5 iPad. Artifacts10845314882/10846147745.
Phone full-screen review contradicts acceptance: Cancel new tag draws beyond its
pill and into explanatory copy; Retry asset types also exceeds its background.
Normal Detail unnecessarily wraps Check out, Add photos and Add item here.
Retained complete captures: command-candidate-rejected-large.png and
command-candidate-rejected-normal.png. No release is authorized by these tests.
Next implementation must size the whole native button around its label; native
label/background containment and ordinary density are explicit screenshot gates.
The modifier approach has used its investigation budget; no more speculative
control-size/fixed-size variations. Evaluate a native control adapter with an
explicit complete-label measurement contract before another implementation.

Replacement decision: local UIKit command-button adapter using native configured
UIButton; same preferred body font and insets determine text/background/host height.
Natural secondary width is constrained to available width; primary fills it. The
existing platform adapter API and Android implementation remain intact. No package
updates. NativeSheetActions uses a separate implementation and is not claimed fixed.
Five bridge checks failed before implementation, then60 focused tests passed;
critic found stale retained callbacks, reproduced separately before adding committed
handler ownership and teardown. Native rendering/compilation is still pending.
